{
  "name": "un.flatten.do",
  "module": "worker.js",
  "version": "1.0.0",
  "description": "UnFlatten JSON Objects",
  "repository": "git@github.com:drivly/un.flatten.do.git",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev worker.js",
    "deploy": "wrangler publish worker.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "dependencies": {
    "flat": "^5.0.2"
  },
  "devDependencies": {
    "jest": "^28.1.3",
    "prettier": "^2.7.1",
    "wrangler": "^2.0.27"
  }
}
